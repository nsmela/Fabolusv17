<UserControl x:Class="Fabolus.Features.Common.Controls.MeshInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Fabolus.Features.Common.Controls"
             d:DataContext="{d:DesignInstance Type=local:MeshInfoViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="200">
    <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch">
        <Label Content="mesh statistics" />
        
        <StackPanel Orientation="Horizontal">
            <Label Content="file path:" Margin="0,0,10,0" />
            <Label Content="{Binding FilePath}" />
        </StackPanel>

        <StackPanel Orientation="Horizontal">
            <Label Content="file size:" Margin="0,0,10,0" />
            <Label Content="{Binding FileSize}" />
        </StackPanel>

        <StackPanel Orientation="Horizontal">
            <Label Content="triangles:" Margin="0,0,10,0" />
            <Label Content="{Binding TriangleCount}" />
        </StackPanel>
        
    </StackPanel>
</UserControl>
